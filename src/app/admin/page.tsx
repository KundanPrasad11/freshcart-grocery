"use client";

import { FormEvent, useCallback, useEffect, useState } from "react";

type Product = { id: string; slug: string; name: string; category: string; price: number; originalPrice: number | null; rating: number; reviews: number; unit: string; badge: string; image: string; description: string; ingredients: string; nutrition: string; stockQuantity: number; featured: boolean; active: boolean };
type Category = { id: string; name: string; emoji: string; description: string; active: boolean };
type Discount = { code: string; type: "percent" | "fixed"; value: number; minimumOrder: number; active: boolean };
type User = { id: string; name: string; email: string; role: "customer" | "admin" };
type Order = { id: string; userId: string; total: number; status: string; address: string; date: string };
type AdminData = { products: Product[]; categories: Category[]; discounts: Discount[]; users: User[]; orders: Order[] };
const blankProduct: Product = { id: "", slug: "", name: "", category: "Fresh Produce", price: 0, originalPrice: null, rating: 0, reviews: 0, unit: "1 unit", badge: "New", image: "https://images.unsplash.com/photo-1542838132-92c53300491e?auto=format&fit=crop&w=900&q=80", description: "", ingredients: "", nutrition: "", stockQuantity: 0, featured: false, active: true };

export default function AdminPage() {
  const [data, setData] = useState<AdminData | null>(null);
  const [product, setProduct] = useState<Product>(blankProduct);
  const [message, setMessage] = useState("Loading dashboard…");
  const load = useCallback(async () => {
    const response = await fetch("/api/admin", { cache: "no-store" });
    const body = await response.json();
    if (!response.ok) { setMessage(body.error ?? "Unable to load admin data."); return; }
    setData(body as AdminData); setMessage("");
  }, []);
  useEffect(() => { void load(); }, [load]);
  const save = async (payload: object) => {
    setMessage("Saving…");
    const response = await fetch("/api/admin", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(payload) });
    const body = await response.json();
    if (!response.ok) { setMessage(body.error ?? "Could not save the change."); return; }
    setMessage("Saved."); await load();
  };
  const submitProduct = (event: FormEvent) => { event.preventDefault(); void save({ action: "product:upsert", product }); };
  if (!data) return <section className="page"><div className="form-card"><h1>Admin dashboard</h1><p>{message}</p></div></section>;
  return <section className="page"><div className="page-intro"><div className="eyebrow">Operations</div><h1>FreshCart admin</h1><p>Manage catalog, inventory, promotions, customers, and fulfillment.</p>{message && <div className="notice">{message}</div>}</div><div className="two-column"><form className="checkout-card" onSubmit={submitProduct}><h2>Catalog & inventory</h2><label className="field"><span>Existing product</span><select value={product.id} onChange={(event) => setProduct(data.products.find((item) => item.id === event.target.value) ?? blankProduct)}><option value="">Create a product</option>{data.products.map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</select></label><div className="payment-fields"><Field label="Product ID" value={product.id} onChange={(value) => setProduct({ ...product, id: value })} /><Field label="Slug" value={product.slug} onChange={(value) => setProduct({ ...product, slug: value })} /></div><Field label="Name" value={product.name} onChange={(value) => setProduct({ ...product, name: value })} /><label className="field"><span>Category</span><select value={product.category} onChange={(event) => setProduct({ ...product, category: event.target.value })}>{data.categories.map((category) => <option key={category.id}>{category.name}</option>)}</select></label><div className="payment-fields"><NumberField label="Price (₹)" value={product.price} onChange={(value) => setProduct({ ...product, price: value })} /><NumberField label="Stock" value={product.stockQuantity} onChange={(value) => setProduct({ ...product, stockQuantity: value })} /></div><Field label="Unit" value={product.unit} onChange={(value) => setProduct({ ...product, unit: value })} /><Field label="Badge" value={product.badge} onChange={(value) => setProduct({ ...product, badge: value })} /><Field label="Image URL" value={product.image} onChange={(value) => setProduct({ ...product, image: value })} /><Field label="Description" value={product.description} onChange={(value) => setProduct({ ...product, description: value })} /><Field label="Ingredients" value={product.ingredients} onChange={(value) => setProduct({ ...product, ingredients: value })} /><Field label="Nutrition" value={product.nutrition} onChange={(value) => setProduct({ ...product, nutrition: value })} /><label><input type="checkbox" checked={product.featured} onChange={(event) => setProduct({ ...product, featured: event.target.checked })} /> Featured</label> <label><input type="checkbox" checked={product.active} onChange={(event) => setProduct({ ...product, active: event.target.checked })} /> Active</label><button className="button" type="submit">Save product</button></form><div className="checkout-card"><h2>Categories</h2><CategoryForm onSave={save} /><h2 className="payment-heading">Discounts</h2><DiscountForm onSave={save} /></div></div><section className="page" style={{ padding: "56px 0" }}><h2>Orders</h2>{data.orders.map((order) => <div className="order-card" key={order.id}><div className="order-top"><div><h3>{order.id} · ₹{order.total}</h3><p>{order.date} · {order.address}</p></div><select value={order.status} onChange={(event) => void save({ action: "order:status", orderId: order.id, status: event.target.value })}>{["Processing", "Packed", "Out for delivery", "Delivered", "Cancelled"].map((status) => <option key={status}>{status}</option>)}</select></div></div>)}<h2 style={{ marginTop: 42 }}>Users</h2>{data.users.map((user) => <div className="order-card" key={user.id}><div className="order-top"><div><h3>{user.name}</h3><p>{user.email}</p></div><select value={user.role} onChange={(event) => void save({ action: "user:role", userId: user.id, role: event.target.value })}><option value="customer">Customer</option><option value="admin">Admin</option></select></div></div>)}</section></section>;
}
function Field({ label, value, onChange }: { label: string; value: string; onChange: (value: string) => void }) { return <label className="field"><span>{label}</span><input required value={value} onChange={(event) => onChange(event.target.value)} /></label>; }
function NumberField({ label, value, onChange }: { label: string; value: number; onChange: (value: number) => void }) { return <label className="field"><span>{label}</span><input required type="number" min="0" value={value} onChange={(event) => onChange(Number(event.target.value))} /></label>; }
function CategoryForm({ onSave }: { onSave: (value: object) => Promise<void> }) { const [category, setCategory] = useState({ id: "", name: "", emoji: "🏷️", description: "", active: true }); return <form onSubmit={(event) => { event.preventDefault(); void onSave({ action: "category:upsert", category }); }}><Field label="Category ID" value={category.id} onChange={(id) => setCategory({ ...category, id })} /><Field label="Name" value={category.name} onChange={(name) => setCategory({ ...category, name })} /><Field label="Emoji" value={category.emoji} onChange={(emoji) => setCategory({ ...category, emoji })} /><Field label="Description" value={category.description} onChange={(description) => setCategory({ ...category, description })} /><button className="button" type="submit">Save category</button></form>; }
function DiscountForm({ onSave }: { onSave: (value: object) => Promise<void> }) { const [discount, setDiscount] = useState({ code: "", type: "percent", value: 10, minimumOrder: 0, active: true }); return <form onSubmit={(event) => { event.preventDefault(); void onSave({ action: "discount:upsert", discount }); }}><Field label="Code" value={discount.code} onChange={(code) => setDiscount({ ...discount, code })} /><label className="field"><span>Type</span><select value={discount.type} onChange={(event) => setDiscount({ ...discount, type: event.target.value })}><option value="percent">Percent</option><option value="fixed">Fixed ₹</option></select></label><NumberField label="Value" value={discount.value} onChange={(value) => setDiscount({ ...discount, value })} /><NumberField label="Minimum order" value={discount.minimumOrder} onChange={(minimumOrder) => setDiscount({ ...discount, minimumOrder })} /><button className="button" type="submit">Save discount</button></form>; }
